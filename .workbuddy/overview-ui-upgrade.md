# 收盘复盘仪表盘 UI 升级 · 工作概览

## 已完成

1. **前端实现审计**：通读 `web/src` 全部页面与组件，结合 `docs/设计约束.md` 找出体系/视觉/动效三层问题。
2. **画布高保真设计稿**：在 Ardot 新建「收盘复盘仪表盘 · 视觉升级方案 v2」（fileId `720361563300601`）。
   - Frame 1：「今日总览 v2」1680×1280 高保真
   - Frame 2：「视觉升级规范 v2」设计 token / Bug 修复 / 8 组微动效参数
3. **书面审计报告**：输出 `docs/UI优化建议与视觉升级方案.md`，含问题定位、改造建议、实施优先级。

## 关键决策

- **不动设计约束红线**：不引入卡片/按钮阴影，不引入第二强调色。
- **指数卡改造**：去掉整圈彩色描边，改为左侧 3px 语义色条 + 同色面积填充。
- **Bug 修复**：行业板块比例条当前把百分比当 px 用（最长仅 100px），方案改为 flex 轨道 + 百分比填充。
- **动效 8 组**：页面入场、数字滚动、条形生长、折线描绘、卡片悬浮、按钮按压、分段滑块、骨架屏 shimmer。

## 下一步

等待用户确认方案与优先级后，进入代码实施阶段：
- 先落地 P0 工程债与 bug 修复；
- 再实现 P1 视觉升级；
- 最后补齐 P2 动效（CSS/JS）。
