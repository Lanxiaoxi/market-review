# 收盘复盘仪表盘 · 技术选型方案（v2）

> 依据：`index.html`（UI 原型）+ `设计约束.md`（单一视觉规范源）
> 目标：把静态原型落成可接真实行情数据的个人复盘 Web 应用
> v2 变更：前端定 **React**（团队熟悉）；数据源定 **Tushare Pro（2000 积分）+ 腾讯分时兜底**

---

## 0. 结论速览

| 层 | 选型 | 一句话理由 |
|---|---|---|
| 前端框架 | **React 18 + TypeScript** | 团队熟悉，生态成熟；纯 SPA 无需 Next.js |
| 构建 | **Vite 6** | 快、配置少，国内镜像友好 |
| 路由 | **react-router v7** | 5 个页面天然对应 5 条路由，支持懒加载 |
| 状态 | **Zustand** | 轻量，跨页面共享：自选池、图表库钉选、行情快照缓存 |
| 图表 | **ECharts 5 + echarts-for-react** | 设计约束 §5 已预留；按需打包，token 映射表见 §4.2 |
| UI 组件 | **自研（原生 CSS + CSS Modules + 设计 token）** | 约束太强（无阴影/单一强调色/禁 spinner），UI 框架反而要对抗 |
| 后端 | **FastAPI + pydantic v2 + httpx** | 复用你 LibreTV / web-chat-app 的既有模式 |
| 环境管理 | **uv**（pyproject.toml + uv.lock） | 单工具管依赖/虚拟环境/锁版本，团队环境一致可复现 |
| 数据源 | **Tushare Pro（2000 积分）为主 + 腾讯接口兜底分时** | 日级数据全覆盖且合规；分时/实时快照腾讯兜底（Tushare 分钟线需 5000 分） |
| 持久化 | **SQLite** | 自选/持仓/图表配置/每日快照，单文件零运维 |
| 部署 | **Caddy/Nginx 反代 + systemd**（或 Docker Compose） | 复用你现有 VPS 模式 |

---

## 1. 需求抽象（从 UI 提炼）

- **形态**：SPA，5 个页面（今日总览/自定义图表/板块轮动/自选跟踪/策略画板），左侧固定 240px 导航。
- **图表清单**（按设计约束 §5 分类）：
  - 多系列分时折线（指数分时对比）
  - sparkline 迷你线（指数卡、表格行内）
  - 柱状分布（涨跌家数区间、成交额分时）
  - 堆叠宽度条（市场宽度：涨/平/跌）
  - 横向条形（行业领涨/领跌）
  - 多系列曲线 + 成交量柱（策略画板）
- **数据特征**：收盘后日级数据，量小（8 指数、31 行业、自选若干）；更新频率低 → **不需要 WebSocket/消息队列，REST 轮询足够**。
- **交互特征**：导航切换、分段控件「只影响本卡」、图表库「钉选到总览页」——全是前端本地状态，无复杂表单流。

## 2. 选型原则

1. **后端复用 FastAPI**：你的 LibreTV、web-chat-app 均为此栈，模式、部署、鉴权经验直接迁移。
2. **前端以「对齐设计约束」为最高优先级**：token 已是现成 CSS 变量（§8），直接作为单一来源，不引 UI 框架（含 Antd，理由见 §4.3）。
3. **图表用 ECharts**：约束文档已写明「若换 ECharts 需对齐配色与线宽」，按映射表封装即可。
4. **数据源以 Tushare 为主**：已购 2000 积分，日级数据（指数/行业/涨跌停/资金流）合规且全覆盖；**仅分时与盘中实时快照用腾讯接口兜底**（Tushare 分钟线需 5000 分）。
5. **复杂度克制**：个人工具，REST + TTL 缓存即可，不上微服务、不上消息队列。

---

## 3. 总体架构

```
┌──────────────────────────────────────────────────────────┐
│                    浏览器 · React18 SPA                    │
│  5 路由页面 ─ react-router ─ Zustand(自选/图表库/快照缓存)   │
│  图表组件层(封装 ECharts, 对齐设计 token)                   │
└──────────────────────────┬───────────────────────────────┘
                           │ REST (JSON) + ETag/时间戳增量
┌──────────────────────────▼───────────────────────────────┐
│                   FastAPI 聚合层                          │
│  routers: /overview /sectors /watchlist /charts /history │
│  services: tushare.py(主) + tencent.py(分时兜底) → 清洗/缓存│
│  tasks: 收盘快照(APScheduler, 15:35)                      │
└───────┬──────────────────────────────┬───────────────────┘
        │                              │
┌───────▼─────────────┐       ┌────────▼────────┐
│ 行情数据源           │       │ SQLite           │
│ Tushare Pro(日级)   │       │ 自选/持仓/图表配置 │
│ 腾讯接口(分时/实时)   │       │ 每日收盘快照(历史) │
└─────────────────────┘       └─────────────────┘
```

---

## 4. 前端技术栈

### 4.1 选型明细

| 项 | 选型 | 理由 |
|---|---|---|
| 框架 | React 18 + TypeScript + Vite 6 | 团队熟悉；Vite 冷启动秒级、产物纯静态 |
| 路由 | react-router v7 | `/overview` `/charts` `/sector` `/watchlist` `/canvas`，路由懒加载（`React.lazy`） |
| 状态 | Zustand | 3 个核心 store：`useMarketStore`（行情快照+刷新节流）、`useWatchlistStore`（自选/持仓）、`useChartLibStore`（图表库+钉选配置）；无样板代码 |
| 数据请求 | TanStack Query + axios | 缓存/重试/轮询开箱即用（`refetchInterval: 60_000` 即盘中轮询），错误提示用 tooltip，符合约束 §7 |
| 图表 | ECharts 5 + `echarts-for-react` | 折线/柱状/堆叠/K线全支持；option 可精确控制颜色线宽 |
| 样式 | CSS Modules + `tokens.css` | 直接从《设计约束.md》§8 复制 CSS 变量；组件按约束文档的「卡片/分段/胶囊/chip」规范手写 |
| 字体 | 系统字体栈 + `tabular-nums` | 不引 web font，符合约束 |

> 说明：选用 TanStack Query 而非裸 axios —— 它内置轮询、缓存去重、错误重试，正好覆盖仪表盘的「收盘后 24h 缓存 / 盘中 60s 轮询」场景，且与 Zustand 分工清晰（Query 管服务端状态，Zustand 管客户端状态）。

### 4.2 图表封装：ECharts option 与设计 token 映射

> 统一封装 `components/charts/BaseChart.tsx`（resize 监听 + 主题注入），具体图表继承它，**禁止在业务代码里散落写死颜色**。token 从 `tokens.css` 读入（`getComputedStyle` 或直接 import 常量），保证改 token 只改一处。

| 设计约束要素 | ECharts 位置 |
|---|---|
| 涨 `#d04545` / 跌 `#2f9e6e` | `series.itemStyle.color` / `series.lineStyle.color`（按数据方向动态取） |
| 多系列色序 ink → accent → purple | `color: ['#1d1d1f', '#0066cc', '#8e6cc9']` |
| 网格线 `#f2f2f4` / 零线 `#e8e8ec` | `xAxis/yAxis.splitLine.lineStyle.color` |
| 折线线宽 2px / sparkline 1.5px | `series.lineStyle.width` |
| 轴刻度 11-12px muted | `axisLabel.fontSize/color` |
| 数字 tabular-nums | `textStyle.fontFamily` 用与全局一致的字体栈 |
| 无阴影/无渐变 | ECharts 默认无阴影，天然符合；柱状用 `itemStyle.borderRadius: [2,2,0,0]` 对齐 4px 圆角 |
| 成交量柱浅蓝灰 `#e8eef7` | 柱状系列直接填该色 |

图表组件清单：`SparklineChart`（迷你线）、`IntradayChart`（分时多线）、`BarDistChart`（涨跌家数分布）、`BreadthBar`（市场宽度堆叠条）、`SectorBar`（行业横向条）、`StrategyChart`（策略曲线+成交量）。

### 4.3 为什么不用 UI 框架（Antd/MUI/Tailwind UI）

- 设计约束红线：**单一强调色 `#0066cc`、禁阴影、禁渐变、禁 spinner、字号阶梯 400/500/600 无 700** —— 主流 UI 框架默认样式几乎全部冲突（Antd 的蓝色系/阴影/Button 动效首当其冲），引入后要花更多精力「对抗」而非「使用」。
- 组件量小（卡片/分段/胶囊/chip/表格行），手写 5-8 个基础组件即可覆盖，成本低且 100% 受控。
- 可选轻量工具：若想少写重复类名，可加 **UnoCSS**（按需生成），但非必需。

### 4.4 状态持久化

- 服务端 SQLite 为主（自选/持仓/图表钉选），登出换设备不丢。
- 本地 `localStorage` 兜底（主题/布局偏好），不存行情数据。

---

## 5. 后端技术栈

| 项 | 选型 | 理由 |
|---|---|---|
| Web 框架 | FastAPI + uvicorn | 与你现有项目一致；async 天然适配 httpx 并发抓取 |
| 数据模型 | pydantic v2 | 接口响应 schema 即类型，可导出 TS 类型供前端复用 |
| 数据源 SDK | `tushare` (pro_api) + httpx | Tushare 拉日级数据；httpx 拉腾讯分时/实时 |
| 缓存 | 内存 TTL 缓存（`cachetools` 或手写 30 行） | 数据量小；收盘后 TTL 24h，盘中 60s 轮询 |
| 定时任务 | APScheduler | 收盘后 15:35 抓全量快照入库（涨跌家数/涨停统计/行业排名），历史复盘秒开 |
| ORM | SQLModel 或 SQLAlchemy | 与你 LibreTV 的 SQLite 模式一致 |
| 环境管理 | **uv**（`uv init` / `uv sync` / `uv run uvicorn`） | 替代 pip+venv；`pyproject.toml` 声明依赖，`uv.lock` 锁版本，国内镜像可在 `pyproject.toml` 配 `[[tool.uv.index]]` 走腾讯 PyPI |
| 鉴权 | 可选 | 个人单用户默认免登；若要多用户直接复用 LibreTV 的 SQLite token 方案 |

### 5.1 数据源选型：Tushare Pro（2000 积分）

Tushare 积分权限按档位开放。**2000 分档位能力映射**（本项目涉及）：

| 数据需求 | Tushare 接口 | 积分要求 | 本项目用途 |
|---|---|---|---|
| 指数日线/基本信息 | `index_daily` / `index_basic` | 120 / 2000 | 8 张指数卡 sparkline、历史走势 |
| 全市场日线行情 | `daily` | 120 | **涨跌家数统计**（一次拉全市场当日，本地 groupby 分档：涨停/涨2-10%/涨0-2%/平/跌0-2%/跌2-10%/跌停） |
| 每日指标（PE/换手/量比/市值） | `daily_basic` | 120 | 自选池指标列 |
| 涨跌停明细 | `limit_list_d` | 2000 | 涨停 TOP5、封板资金 |
| 个股/板块资金流 | `moneyflow` | 2000 | 板块资金排名 |
| 申万行业分类+日线 | `index_classify` / `sw_daily` | 2000 | 板块轮动页（申万一级 31 行业） |
| 股票列表 | `stock_basic` | 120 | 自选池代码校验、名称映射 |
| **日内分时** | `stock_mins` | **需 5000 分** ❌ | 2000 分拿不到 → 腾讯兜底（见下） |

**腾讯接口兜底（仅两处）**：
| 数据需求 | 腾讯接口 | 说明 |
|---|---|---|
| 指数分时主图（当日分钟线） | `web.ifzq.gtimg.cn/appstock/app/minute/query?code=sh000001` | 免费、返回当日分时 JSON；收盘后数据已固定 |
| 盘中实时快照 | `qt.gtimg.cn/q=sh000001,sz399001,...` | 批量实时价（可选，仅做盘中 60s 刷新时用） |

**频率与调用量**：Tushare 2000 分约 500 次/分钟；本项目日快照任务调用量 < 50 次/日，**远低于限额**，无需并发控制。日级数据收盘后不再变化，快照入库后历史页直读 SQLite。

> 合规说明：Tushare 为积分制授权数据，主数据源合规；腾讯接口仅作分时补充。页脚文案从「数据为示意样例」改为「数据来源：Tushare / 腾讯行情，仅供参考」。

### 5.2 路由设计

```
GET /api/overview          → 指数卡 + 分时对比 + 市场宽度 + 涨停TOP + 行业TOP5
GET /api/sectors?sort=pct  → 申万一级行业排名表
GET /api/watchlist         → 自选池 + 持仓盈亏汇总
GET /api/charts            → 图表库列表 + 钉选配置
GET /api/history?date=     → 历史收盘快照（复盘对比）
POST/PUT/DELETE /api/watchlist、/api/charts → 用户数据 CRUD
```

### 5.3 缓存与实时性策略

- **收盘后（15:00+）**：日级数据不变 → 快照库直接读，接口无计算。
- **盘中（可选）**：前端 TanStack Query 60s 轮询 `/api/overview`，后端 TTL 缓存 60s，ETag/时间戳去重。
- 不做 WebSocket：个人工具 + 低频数据，轮询足够且实现简单。

---

## 6. 数据流链路

```
[Tushare pro_api] --index_daily/daily/limit_list_d/sw_daily--> [FastAPI tushare.py 清洗/归一]
[腾讯分钟接口] --httpx--> [FastAPI tencent.py 分时兜底]
      --> [TTL 缓存(60s/24h)] --> [GET /api/overview] --> [TanStack Query]
      --> [Zustand market store] --> [BaseChart 组件注入 token] --> [ECharts 渲染]
      --> [用户交互: 分段切换] --> 仅重绘本图 option(局部联动)

[收盘快照任务 15:35] --> [SQLite snapshot] --> [GET /api/history] --> [历史对比页]

[自选/持仓/图表钉选] --REST CRUD--> [SQLite] <--Zustand watchlist/chartLib--> [UI]
```

---

## 7. 项目文件目录树

> 目标结构（文件级），M1-M4 按此逐步落盘。`index.html` 为现有 UI 原型，保留作设计参照。

```
market-review/
├── web/                                  # 前端 (React18 + Vite + TS)
│   ├── public/
│   │   └── favicon.svg
│   ├── src/
│   │   ├── main.tsx                      # 入口：挂载 App + Router
│   │   ├── App.tsx                       # 根布局：Sidebar + <Outlet/>
│   │   ├── router/
│   │   │   └── index.tsx                 # 5 路由 + React.lazy 懒加载
│   │   ├── api/                          # 接口层（TS 类型与后端 pydantic 对齐）
│   │   │   ├── client.ts                 # axios 实例（baseURL/超时/错误拦截）
│   │   │   ├── overview.ts               # GET /api/overview
│   │   │   ├── sectors.ts                # GET /api/sectors
│   │   │   ├── watchlist.ts              # CRUD /api/watchlist
│   │   │   ├── charts.ts                 # CRUD /api/charts
│   │   │   └── history.ts                # GET /api/history
│   │   ├── stores/                       # Zustand（客户端状态）
│   │   │   ├── market.ts                 # 行情快照 + 刷新节流
│   │   │   ├── watchlist.ts              # 自选/持仓（与后端同步）
│   │   │   └── chartLib.ts               # 图表库 + 钉选配置
│   │   ├── hooks/                        # TanStack Query（服务端状态）
│   │   │   ├── useOverview.ts
│   │   │   ├── useSectors.ts
│   │   │   └── usePolling.ts             # 盘中 60s 轮询开关
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── Sidebar.tsx           # 240px 侧栏导航（含 .module.css）
│   │   │   │   └── PageHeader.tsx        # 页面标题栏
│   │   │   ├── common/
│   │   │   │   ├── BaseCard.tsx          # 白底发丝边卡片
│   │   │   │   ├── Segmented.tsx         # 分段控件（只影响本卡）
│   │   │   │   ├── PillButton.tsx        # 胶囊主按钮（scale 0.96 按压）
│   │   │   │   ├── Chip.tsx              # 状态 chip
│   │   │   │   ├── IndexCard.tsx         # 指数卡（数值+涨跌+sparkline）
│   │   │   │   ├── DataTable.tsx         # 行情表格行（单位标注列头）
│   │   │   │   └── PlaceholderCard.tsx   # 占位卡（选择图表）
│   │   │   └── charts/
│   │   │       ├── BaseChart.tsx         # ECharts 封装（resize + token 注入）
│   │   │       ├── SparklineChart.tsx    # 迷你线 1.5px
│   │   │       ├── IntradayChart.tsx     # 分时多线
│   │   │       ├── BarDistChart.tsx      # 涨跌家数分布直方图
│   │   │       ├── BreadthBar.tsx        # 市场宽度堆叠条
│   │   │       ├── SectorBar.tsx         # 行业横向条形
│   │   │       └── StrategyChart.tsx     # 策略曲线 + 成交量柱
│   │   ├── pages/
│   │   │   ├── OverviewPage.tsx          # 今日总览
│   │   │   ├── CustomChartPage.tsx       # 自定义图表（一行两卡）
│   │   │   ├── SectorPage.tsx            # 板块轮动
│   │   │   ├── WatchlistPage.tsx         # 自选跟踪
│   │   │   └── StrategyPage.tsx          # 策略画板
│   │   ├── styles/
│   │   │   ├── tokens.css                # 设计约束 §8 CSS 变量（唯一来源）
│   │   │   ├── global.css                # 重置 + 字体栈 + tabular-nums
│   │   │   └── modules/                  # 各组件 *.module.css
│   │   └── types/
│   │       └── market.ts                 # 行情/板块/自选 TS 类型
│   ├── index.html
│   ├── vite.config.ts                    # @ alias + 按需 ECharts + dev 代理 /api→:8000
│   ├── tsconfig.json
│   └── package.json
├── api/                                  # 后端 (FastAPI)
│   ├── app/
│   │   ├── main.py                       # FastAPI 实例 + 路由注册 + CORS
│   │   ├── config.py                     # 环境变量（TUSHARE_TOKEN 等）
│   │   ├── cache.py                      # 内存 TTL 缓存（60s/24h）
│   │   ├── tasks.py                      # APScheduler 收盘快照（15:35）
│   │   ├── models/
│   │   │   ├── db.py                     # SQLModel engine/session 初始化
│   │   │   ├── watchlist.py              # 自选/持仓表
│   │   │   ├── chart_config.py           # 图表库/钉选配置表
│   │   │   └── snapshot.py               # 每日收盘快照表
│   │   ├── schemas/                      # pydantic 响应模型
│   │   │   ├── overview.py
│   │   │   ├── sector.py
│   │   │   ├── watchlist.py
│   │   │   └── charts.py
│   │   ├── services/
│   │   │   ├── tushare_provider.py       # 主源：Tushare 日级数据
│   │   │   ├── tencent_provider.py       # 兜底：腾讯分时/实时快照
│   │   │   ├── aggregator.py             # 清洗/归一/涨跌家数分档统计
│   │   │   └── snapshot_service.py       # 快照入库 + 历史查询
│   │   └── routers/
│   │       ├── overview.py               # GET /api/overview
│   │       ├── sectors.py                # GET /api/sectors?sort=
│   │       ├── watchlist.py              # CRUD /api/watchlist
│   │       ├── charts.py                 # CRUD /api/charts
│   │       └── history.py                # GET /api/history?date=
│   ├── data/                             # app.db（SQLite，gitignore）
│   ├── tests/                            # pytest：aggregator 单测 + 接口冒烟
│   ├── .env.example                      # TUSHARE_TOKEN=xxx（真实值不入库）
│   ├── pyproject.toml                    # uv 依赖声明（含国内镜像 index 配置）
│   └── uv.lock                           # uv 锁文件（提交进 git，保证环境可复现）
├── deploy/
│   ├── Caddyfile                         # / → web/dist，/api → :8000
│   ├── market-api.service                # systemd unit（uvicorn）
│   └── Dockerfile                        # 可选容器化（前后端合并镜像）
├── docs/
│   ├── 设计约束.md                        # 视觉规范单一来源
│   ├── 技术选型.md                        # 本文档
│   └── 任务拆分规划.md                    # 任务拆解与依赖（已存在，M1-M4 对照执行）
└── index.html                            # 现有 UI 原型（设计参照，勿删）
```

> `.gitignore` 至少包含：`api/data/`、`web/dist/`、`node_modules/`、`.env`、`__pycache__/`。

---

## 8. 部署方案

- **单机 VPS**（复用你现有模式）：前端 `vite build` 产物 → 静态目录；后端 `uv sync` 部署后，systemd 以 `.venv/bin/uvicorn` 启动（或 Docker Compose）。
- **Caddy/Nginx 反代**：`/` → 前端静态，`/api/*` → FastAPI，同域无 CORS 问题。
- 备份：SQLite 单文件每日 rsync/快照即可。
- Tushare token 存环境变量（`.env`，不进 git）。

---

## 9. 备选路线：Vue（如团队后续倾向）

> v2 起主推 React，Vue 降为备选，仅作对照保留。

| 层 | React 版 | Vue 等价 |
|---|---|---|
| 框架 | React 18 | Vue 3.5 + TS |
| 路由 | react-router v7 | Vue Router 4 |
| 状态 | Zustand | Pinia |
| 数据请求 | TanStack Query | TanStack Vue Query |
| 图表/样式/后端 | 不变 | 不变 |

- **不建议 Next.js/Nuxt**：纯仪表盘 SPA，无 SSR/SEO 需求，Vite SPA 更轻。

---

## 10. 里程碑（小步快跑，每步可验证）

| 里程碑 | 内容 | 验证标准 |
|---|---|---|
| M1 静态复刻 | Vite+React 脚手架 + tokens.css + 5 页面组件化，静态 mock 数据 | 页面与 index.html 视觉 1:1 |
| M2 数据接入 | Tushare services + 腾讯分时兜底 + 5 组接口 + 前端替换 mock | 总览页展示真实行情 |
| M3 交互闭环 | 自选/持仓 CRUD、图表库钉选、分段联动、盘中轮询 | 交互符合约束 §7 |
| M4 历史与部署 | 收盘快照任务 + 历史复盘 + Caddy 部署 | 15:35 后历史页可查 |

---

## 11. 关键决策记录与待确认

**已定**：
1. ✅ 前端框架：**React 18 + TS + Vite**（团队熟悉）
2. ✅ 数据源：**Tushare Pro 2000 积分**（日级主源）+ **腾讯接口兜底**（分时/实时快照，因 Tushare 分钟线需 5000 分）

**待确认**：
3. **鉴权**：个人单用户免登（默认），还是复用 LibreTV 的 SQLite 多用户 token 体系？
4. **实时性**：仅收盘后数据（默认），还是需要盘中 60s 轮询刷新？
