# 任务：市场宽度表格视觉优化

## 目标
优化 `OverviewPage`（今日总览页）市场宽度卡片的视觉表现，**严格遵守 `docs/设计约束.md` 令牌体系**（红涨绿跌、发丝边、无阴影、tabular-nums），通过 Ardot 画布交付 1:1 高保真设计稿。

## 现状问题
1. 家数大数字、堆叠条、百分比行说的是同一信息，**三处割裂编码**
2. 堆叠条缺少多空力量参考线
3. 成交额 44px 巨型数字脱离「汇总卡指标值 24px」字号阶梯
4. 涨停/跌停一行弱化 13px 小字被埋没

## 设计稿产出
- 画布文件：「市场宽度表格视觉优化」 (Ardot fileId 720094072867792)
- 卡宽严格 400px（与设计约束 §2.6「市场宽度卡宽 400」一致）
- 核心改造：
  - **表格化重构**：状态（色点+名）｜分布条（0–100% 轨道+占比填充）｜占比｜家数，四列结构
  - 堆叠条新增 50% 多空参考刻度
  - 成交额 44→24px，右对齐与标签同行
  - 涨停/跌停升格为对等指标块（19px 语义色大数字 + 竖发丝线分隔）
- 全部沿用设计令牌：#d04545/#2f9e6e/#ececef/#f5f5f7，无阴影、无渐变

## 验证
- 截图通过：表格行内分布条、堆叠条分段、字号阶梯、配色均符合规范
- 与现有 `OverviewPage.tsx` 卡片一一对应，可直接作为代码改动的视觉参照

## 后续
如需实施到代码，建议在 `OverviewPage.tsx`（line 278-321）和 `HistoryPage.tsx`（line 104-128）的市场宽度卡按此设计稿重构 DOM 结构与样式。
