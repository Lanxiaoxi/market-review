name: runner-smoke

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: 环境信息
        run: |
          echo "runner 接单成功 ✅"
          whoami
          uname -sr
          nproc
          hostname
          pwd
